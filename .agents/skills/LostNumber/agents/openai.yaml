interface:
  display_name: "LostNumber"
  short_description: "Repo-specific patterns and workflows for LostNumber"
  default_prompt: "Use the LostNumber repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true